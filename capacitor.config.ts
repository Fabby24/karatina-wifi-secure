import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fabian.securelabwifi',
  appName: 'SecureLab Wifi',
  webDir: 'dist'
};

export default config;
